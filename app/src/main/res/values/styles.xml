<resources>

    <style name="MyTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        ...</style>

    <style name="MyTheme.NoActionBar">
        <!-- Both of these are needed -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>

    </style>

    <style name="DialogTransparentTheme" parent="@android:style/Theme.Dialog">
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="DialogTransparentThemeDimFalse" parent="@android:style/Theme.Dialog">
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="Theme.Transparent" parent="@android:style/Theme.Translucent.NoTitleBar"></style>

    <style name="MyTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="MyTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ProgressBarStyle" parent="@android:style/Widget.Holo.Light.ProgressBar.Inverse" />

    <style name="Theme.FullScreen" parent="@android:style/Theme.Holo">
        <item name="android:windowNoTitle">false</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
